<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keyou.fdcda.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.keyou.fdcda.model.User">
        <result column="ID" jdbcType="DECIMAL" property="id" />
        <result column="NAME" jdbcType="VARCHAR" property="name" />
        <result column="CREDIT" jdbcType="DECIMAL" property="credit" />
        <result column="PASSWORD" jdbcType="VARCHAR" property="password" />
        <result column="CREATED_AT" jdbcType="TIMESTAMP" property="createdAt" />
        <result column="UPDATED_AT" jdbcType="TIMESTAMP" property="updatedAt" />
    </resultMap>
    <insert id="insert" parameterType="com.keyou.fdcda.model.User">
        insert into HS_USER (ID, NAME, CREDIT,
        PASSWORD, CREATED_AT, UPDATED_AT
        )
        values (#{id,jdbcType=DECIMAL}, #{name,jdbcType=VARCHAR}, #{credit,jdbcType=DECIMAL},
        #{password,jdbcType=VARCHAR}, #{createdAt,jdbcType=TIMESTAMP}, #{updatedAt,jdbcType=TIMESTAMP}
        )
    </insert>
    <select id="getUserById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select ID, NAME, CREDIT, PASSWORD, CREATED_AT, UPDATED_AT from HS_USER where ID = #{id}
    </select>
    <insert id="insertSelective" parameterType="com.keyou.fdcda.model.User">
        insert into HS_USER
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                ID,
            </if>
            <if test="name != null">
                NAME,
            </if>
            <if test="credit != null">
                CREDIT,
            </if>
            <if test="password != null">
                PASSWORD,
            </if>
            <if test="createdAt != null">
                CREATED_AT,
            </if>
            <if test="updatedAt != null">
                UPDATED_AT,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=DECIMAL},
            </if>
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="credit != null">
                #{credit,jdbcType=DECIMAL},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR},
            </if>
            <if test="createdAt != null">
                #{createdAt,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedAt != null">
                #{updatedAt,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
</mapper>